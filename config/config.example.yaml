server:
  port: "8080"
  host: "127.0.0.1"

auth:
  username: "admin"
  password: "changeme"
  session_secret: "replace-this-with-random-secret"

security:
  allowed_ips: []
  require_2fa: false
  safe_mode: true
